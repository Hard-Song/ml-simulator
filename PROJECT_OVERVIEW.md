# ML Simulator Web - 项目总览

## 项目完成情况

### 核心功能模块

| 模块 | 文件 | 状态 | 说明 |
|------|------|------|------|
| 模拟引擎 | `ml_simulator.py` | ✅ 完成 | 核心模拟框架 |
| Flask 后端 | `app.py` | ✅ 完成 | RESTful API |
| 前端主页面 | `templates/index.html` | ✅ 完成 | 多模型对比界面 |
| 前端交互 | `static/js/index.js` | ✅ 完成 | JavaScript 交互逻辑 |
| 前端样式 | `static/css/style.css` | ✅ 完成 | 自定义样式 |

### 工具和文档

| 文件 | 说明 |
|------|------|
| `requirements.txt` | Python 依赖列表 |
| `uv.toml` | UV 配置（国内镜像） |
| `start_web.bat` | Windows 启动脚本 |
| `start_web.sh` | Linux/Mac 启动脚本 |
| `test_dependencies.py` | 依赖测试脚本 |
| `README_ML_SIMULATOR.md` | 核心框架文档 |
| `README_WEB.md` | Web 应用详细文档 |
| `QUICKSTART.md` | 快速启动指南 |
| `example_ml_simulator.py` | 7个使用示例 |
| `test_ml_simulator.py` | 9个测试用例 |

## 快速开始

### 1. 启动服务器

```bash
# Windows
双击 start_web.bat

# Linux/Mac
./start_web.sh

# 或手动
uv venv
uv pip install -r requirements.txt
uv run python app.py
```

### 2. 访问应用

打开浏览器访问: http://localhost:5000/

## 功能特性

### 已实现功能 ✅

1. **多模型对比**
   - 支持10种预定义模型
   - 实时参数调整
   - 表格 + 3种图表展示
   - CSV 数据导出

2. **任务类型**
   - 二分类
   - 多分类
   - 回归

3. **难度控制**
   - 类间可分性
   - 标签噪声
   - 特征噪声
   - 非线性强度

4. **可视化**
   - 准确率柱状图
   - AUC 柱状图
   - 综合性能雷达图

### 计划中功能 🚧

1. **学习曲线页面**
   - 不同数据规模下的性能
   - 多模型学习效率对比

2. **难度扫描页面**
   - 参数敏感性分析
   - 交互式扫描工具

3. **高级可视化**
   - ROC 曲线
   - PR 曲线
   - 混淆矩阵

## API 端点

### 公共接口

| 端点 | 方法 | 功能 |
|------|------|------|
| `/` | GET | 主页（多模型对比） |
| `/api/models` | GET | 获取模型列表 |
| `/api/simulate` | POST | 执行模拟 |
| `/api/export/csv` | POST | 导出 CSV |

### API 示例

```python
import requests

# 执行模拟
response = requests.post('http://localhost:5000/api/simulate', json={
    'task_type': 'binary',
    'num_samples': 5000,
    'n_classes': 2,
    'models': ['lgbm', 'rf', 'logreg'],
    'difficulty': {
        'separability': 0.7,
        'label_noise': 0.05,
        'feature_noise': 0.1,
        'nonlinearity': 0.3,
        'spurious_correlation': 0.3,
    },
    'random_state': 42,
})

results = response.json()
print(results)
```

## 技术架构

```
┌─────────────────────────────────────┐
│         Browser (Frontend)          │
│  ┌───────────────────────────────┐  │
│  │ Bootstrap 5 + Chart.js        │  │
│  │ - index.html                  │  │
│  │ - style.css                   │  │
│  │ - index.js                    │  │
│  └───────────────┬───────────────┘  │
└──────────────────┼───────────────────┘
                   │ HTTP/REST API
┌──────────────────▼───────────────────┐
│      Flask Server (Backend)         │
│  ┌───────────────────────────────┐  │
│  │ app.py (RESTful API)          │  │
│  │ - /api/simulate               │  │
│  │ - /api/models                 │  │
│  │ - /api/export/csv             │  │
│  └───────────────┬───────────────┘  │
└──────────────────┼───────────────────┘
                   │
┌──────────────────▼───────────────────┐
│      ML Simulator Core              │
│  ┌───────────────────────────────┐  │
│  │ ml_simulator.py               │  │
│  │ - TaskConfig                  │  │
│  │ - DifficultyConfig            │  │
│  │ - MLSimulator                 │  │
│  │ - compare_models()            │  │
│  │ - compute_metrics()           │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

## 项目统计

- **Python 代码**: ~2000 行
- **JavaScript 代码**: ~400 行
- **HTML/CSS**: ~300 行
- **总文件数**: 15+ 个
- **支持模型**: 10 种
- **测试用例**: 9 个（全部通过）

## 开发环境

- **Python**: 3.10+
- **包管理**: uv
- **Web 框架**: Flask 3.0+
- **前端框架**: Bootstrap 5
- **图表库**: Chart.js 4.4
- **数据科学**: NumPy 2.0+, Pandas 2.0+

## 贡献指南

欢迎提交问题和改进建议！

### 开发流程

1. Fork 项目
2. 创建功能分支
3. 提交更改
4. 推送到分支
5. 创建 Pull Request

## 许可证

MIT License

## 联系方式

如有问题或建议，请提交 Issue。
